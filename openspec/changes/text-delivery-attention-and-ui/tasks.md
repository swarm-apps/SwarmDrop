## 1. 文本领域边界与回归基础

- [x] 1.1 将 `crates/transfer` 根目录的文本记录、RPC wire、确认状态机和服务重组到 `text_delivery/` 垂直模块，并移除旧模块路径的兼容导出。
- [x] 1.2 收窄 `lib.rs` 的文本公开面，更新 core、Tauri、UniFFI 和存储调用方，确保协议及既有文本账本行为不变。
- [x] 1.3 为模块重组后的文本校验、编码、出站状态转换和存储端口补齐单元与协议回归测试。

## 2. 事件驱动的文本注意力

- [x] 2.1 定义不含正文、以投递标识和注意力种类关联的文本注意力领域事件，并经 transfer/core EventBus 暴露给宿主。
- [x] 2.2 在待确认记录持久化后以及自动接收记录与收件箱投影持久化后发射事件；事件失败不得回滚投递数据。
- [x] 2.3 为 pending 确认队列实现投递与状态去重、FIFO 展示顺序、accept/reject 原子竞争和过期恢复，并保留读取同步作为事件遗漏后的协调路径。
- [x] 2.4 为事件发生顺序、重复 RPC、连续待确认、并发 accept/reject、事件发射失败与超时添加领域测试。

## 3. 共享文本体验语义

- [x] 3.1 在 `packages/shared-view` 定义文本模式、UTF-8 字节/`KiB` 格式化、发送记录状态、确认队列项与收件箱定位的纯 view-model 和本地化键。
- [x] 3.2 为共享模型建立跨端 fixture 与单元测试，覆盖多字节文本、临界值、超限、可重试状态及无正文的通知内容。
- [x] 3.3 以 mutation testing 或等价杀伤用例验证：删除事件去重、改用字符数计量、颠倒持久化/发射顺序、允许并发确认终局时，测试必须失败。

## 4. 三端前台文本接收与发送表面

- [x] 4.1 在桌面、Web 和移动接入文本注意力事件，统一实现“一个确认面板 + 后续队列”以及自动接收的非阻断反馈与收件箱失效。
- [x] 4.2 将三端文本编辑器收敛到共享信息顺序和状态语义：紧凑等宽的文件/文本分段控件、显式粘贴/清空、统一 `KiB` 限制、最近发送状态和主要提交操作。
- [x] 4.3 为桌面与 Web 的确认面板实施键盘焦点管理、读屏标签与安全点击目标；为移动验证触摸目标和安全区内提交操作。
- [ ] 4.4 为每个宿主添加文本发送、接收、确认、拒绝、自动接收、超限、重试及从反馈定位收件箱的 UI 测试。

## 5. 桌面后台通知

- [x] 5.1 扩展桌面 core 事件消费与 `DesktopNotifier`，仅在窗口未聚焦时为文本注意力发送原生通知。
- [x] 5.2 实现通知激活后导向收件箱的路由意图，并确保通知标题/正文不含文本内容。
- [x] 5.3 使用可控焦点与通知 fake 覆盖聚焦抑制、未聚焦发送、通知失败不影响投递和隐私正文约束。

## 6. Web 浏览器通知与降级

- [x] 6.1 实现 Web 文本通知适配器，检查 API 支持、权限、页面可见性与焦点，并将通知激活路由到收件箱。
- [x] 6.2 在 Web 设置中提供用户手势触发的文本通知授权入口，展示授权、拒绝与不可用状态，且事件路径不得请求权限。
- [x] 6.3 覆盖已授权隐藏页通知、可见页仅应用内反馈、权限拒绝/API 不可用和页面重启后收件箱协调；不实现关闭页面后的离线推送。

## 7. 集成验证与视觉回归

- [x] 7.1 执行 Rust 格式化、workspace 全目标检查、WASM 检查、TypeScript 类型检查及文本相关 Rust/前端/移动测试。
- [ ] 7.2 使用 Tauri MCP 和浏览器自动化在真实桌面与 Web 界面验证发送布局、确认面板、后台/失焦通知与收件箱定位，并保留可复现的测试步骤。
- [ ] 7.3 在桌面宽屏、Web 窄/宽屏和移动窄屏进行视觉与可访问性回归，确认文本表面遵守本 change 的统一契约且不触及全产品 UI 审计范围。
