# fix-clipboard-native-read 任务分解

## Phase 0 — 原生读路径

- [x] `src-tauri/capabilities/default.json` 加 `clipboard-manager:allow-read-text`
      （标识有效性已核：`gen/schemas/{desktop,macOS}-schema.json` 里存在，
      对应 plugin 的 `permissions/autogenerated/commands/read_text.toml`）
- [x] `src/lib/clipboard.ts` 加 `readText(): Promise<string>`，与 `copyText` 对称收口；
      文档注释记下「错误不可归因」（arboard `ContentNotAvailable` 把空/非文本/无权限压成一种）
- [x] `use-clipboard-invite.ts` 改走 `readText()`；`catch` 分支加 `console.debug` 留痕
      —— 原来的静默 `catch { return }` 正是那条错误知识库记录长期不被证伪的原因
- [ ] 冒烟：桌面复制邀请 → 切窗回来 → 一键条出现；确认无权限弹窗
      **延后到 `invite-url-canonical` 之后统一验证** —— 那个 change 会把 `INVITE_PREFIX`
      从 `sd:` 改成 canonical base URL，现在跑的结果会被它推翻

## Phase 1 — 防回归

- [x] `scripts/check-clipboard-access.mjs`：扫 `src/**/*.{ts,tsx}` 命中
      `navigator.clipboard.<方法>(` 即非零退出并打印文件:行号。**只扫仓库根 `src/`**
      —— `docs/`（Web 端）用 `navigator.clipboard` 是唯一正确选择，扫它会误拦
- [x] `package.json` 加 `"check:clipboard"`
- [x] 自测：临时探针文件确认能拦（exit=1 + 定位到行），删除后恢复 exit=0

## Phase 2 — 门禁

- [x] `pnpm exec tsc --noEmit` 通过
- [x] `pnpm check:clipboard` 通过
- [x] 连带跑了 `pnpm test`（64 passed）与 `pnpm check:zustand-access`（58 allowlisted）
- [x] 知识库：`dev-notes/knowledge/theme-and-styling.md` 的「剪贴板」一节
      **推翻了原「读剪贴板例外」条目**并留下教训（静默 catch + 未实测的平台行为假设
      = 能在知识库里活很久的错误记录）

## 实施中对 artifacts 的修正

- `specs/clipboard-native-access/spec.md`：原写「读失败分支与『剪贴板为空』可区分」，
  实施时发现 API 层做不到（arboard 对空剪贴板同样返回 `ContentNotAvailable`），
  已改为「不可区分 + 调用方不得据错误推断原因」。
- `proposal.md` 的 Why 把现状描述为「一半守住一半漏了」，实际上
  `dev-notes/knowledge/theme-and-styling.md:335` 有一条明确的「读剪贴板例外」决策 ——
  不是遗漏而是**有记录但理由错误**的决策。结论不变（仍该改），根因描述更准确了。
