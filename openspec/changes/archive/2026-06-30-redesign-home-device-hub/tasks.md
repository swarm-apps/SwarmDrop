## 1. 顶栏历史入口

- [x] 1.1 在 `src/components/layout/app-topbar.tsx` 的设置按钮左侧新增传输历史图标按钮，导航到 `/transfer`
- [x] 1.2 为历史入口补齐可访问标签、hover/focus 样式，并确保位于窗口控制按钮之前
- [x] 1.3 确认 `/transfer` 和 `/transfer/$sessionId` 的面包屑显示仍符合当前路由层级

## 2. 首页数据分组

- [x] 2.1 在 `/devices` 首页中派生 `nearbyDevices`，筛选未配对且在线的运行时设备
- [x] 2.2 保留并整理已配对设备合并逻辑，运行时在线数据优先，持久化设备作为离线回退
- [x] 2.3 为已配对设备排序，在线设备排在离线设备之前
- [x] 2.4 从 `transfer-store.sessions` 派生活跃传输列表，并按开始时间倒序排列

## 3. 首页区域组件

- [x] 3.1 创建或抽取“附近设备”区域，展示附近未配对设备、空状态和行内配对操作
- [x] 3.2 创建或抽取“快速配对”区域，提供“生成配对码”和“输入配对码”两个入口
- [x] 3.3 调整“已配对设备”区域，突出在线发送目标，并保留离线设备状态与管理操作
- [x] 3.4 创建“正在传输”区域，仅展示活跃传输会话，并为空状态提供低占用展示
- [x] 3.5 从首页移除 `HomeRecentTransfers` 渲染，确保历史记录不再出现在首页

## 4. 布局与状态

- [x] 4.1 将桌面首页重排为设备中心布局，包含附近设备、快速配对、已配对设备和正在传输区域
- [x] 4.2 保持节点未运行时的离线空状态，并调整文案围绕启动后发现设备和接收文件
- [x] 4.3 确保首页在附近设备为空、已配对设备为空、活跃传输为空时均有合理空状态
- [x] 4.4 确保首页布局在窄桌面宽度下不发生文字重叠或按钮换行异常

## 5. 国际化与验证

- [x] 5.1 为新增和调整的中文文案使用 Lingui 提取宏
- [x] 5.2 运行 `pnpm i18n:extract` 更新翻译目录
- [x] 5.3 运行 `pnpm build` 验证 TypeScript 与 Vite 构建
- [x] 5.4 使用 Tauri MCP 检查运行中的首页 DOM 与截图，确认首页不再展示最近传输历史
- [x] 5.5 验证顶栏历史入口可进入 `/transfer`，且首页活跃传输区域只展示 `transfer-store.sessions`

## 6. 主页配对交互优化

- [x] 6.1 将生成配对码改为首页行内卡片，展示活跃配对码、倒计时、复制和重新生成操作
- [x] 6.2 将输入配对码改为首页弹窗，复用 OTP 输入、查找设备和发送配对请求流程

## 7. 首页视觉协调优化

- [x] 7.1 将首页重构为设备中心视觉层级，增加概览区、分区壳层和更克制的桌面背景
- [x] 7.2 统一附近设备、快速配对、已配对设备和正在传输区域的圆角、描边、阴影与交互反馈
- [x] 7.3 重做设备卡片视觉，包含状态胶囊、连接信息、主动作按钮和列表态样式
- [x] 7.4 调整首页信息架构，将已配对设备作为首个内容卡片，并合并附近设备与配对码为添加设备卡片
