## 1. 数据模型

- [x] 1.1 新增 `inbox_items` 和 `inbox_item_files` SeaORM entity model，并按需要增加 source/content kind 枚举。
- [x] 1.2 新增收件箱表、索引，以及基于 `transfer_session_id` 的幂等约束迁移。
- [x] 1.3 增加 Inbox item file 与 Inbox item、可选 transfer file/session 的实体关系。
- [x] 1.4 增加 migration 测试或 schema 检查，覆盖收件箱表创建和回滚。

## 2. Core 收件箱操作

- [x] 2.1 增加从已完成接收会话创建收件箱条目的数据库操作。
- [x] 2.2 保证同一接收会话重复触发完成路径时收件箱创建幂等。
- [x] 2.3 增加列出收件箱条目、加载详情、归档/删除记录、标记本地文件缺失的数据库操作。
- [x] 2.4 增加测试，证明清理活动记录不会删除收件箱条目或已接收文件。

## 3. 接收完成路径集成

- [x] 3.1 在接收完成且文件完成落盘/校验之后创建收件箱条目。
- [x] 3.2 确保暂停、中断、失败、拒绝、取消的接收会话不会创建收件箱条目。
- [x] 3.3 增加修复/补建 helper，用于给已完成但缺少收件箱条目的接收会话补建记录。
- [x] 3.4 增加“接收完成 → 创建收件箱条目”的单元或集成测试。

## 4. Tauri Commands 和 Host 操作

- [x] 4.1 新增列出收件箱条目和加载收件箱详情的 typed commands。
- [x] 4.2 新增通过 host 平台打开、显示收件箱条目/文件的 typed commands。
- [x] 4.3 新增将收件箱内容导出/复制到用户选择位置的 typed commands。
- [x] 4.4 新增归档/删除收件箱条目的 typed commands，并提供独立的“删除本地文件”选项。
- [x] 4.5 重新生成并验证 TypeScript bindings。

## 5. 桌面端 UI

- [x] 5.1 新增收件箱路由和导航入口，聚焦已接收内容。
- [x] 5.2 实现收件箱列表 UI，展示来源、接收时间、条目数量、总大小和 missing 状态。
- [x] 5.3 实现收件箱详情 UI，展示文件列表、checksum、来源元数据和关联传输详情。
- [x] 5.4 接入收件箱操作：打开、显示位置、导出、归档/删除，以及可选删除本地文件确认。
- [x] 5.5 将传输历史页改名/重塑为活动与恢复。
- [x] 5.6 将活动与恢复拆分为活跃、可恢复、失败/取消、可选已完成诊断几个区块。
- [x] 5.7 更新空状态和文案，让收件箱与活动含义清楚分离。

## 6. 保留和删除语义

- [x] 6.1 修改清空历史/活动行为，确保它不能删除收件箱记录或已接收文件。
- [x] 6.2 为从收件箱删除本地文件增加明确的破坏性确认文案。
- [ ] 6.3 增加测试覆盖：仅删除收件箱记录、删除收件箱记录和文件、清空活动记录。

## 7. 文档和验证

- [x] 7.1 更新 `dev-notes/ai-era-product-directions.md` 或相关产品文档中的收件箱/活动最终文案。
- [x] 7.2 运行 `openspec validate drop-inbox-and-transfer-activity`。
- [x] 7.3 运行数据库操作和接收完成创建收件箱相关的 Rust 测试。
- [x] 7.4 增加路由和 bindings 后运行前端 typecheck 及相关 UI 测试。
- [ ] 7.5 手动验证：完成接收进入收件箱，暂停接收只出现在活动与恢复中，清空活动不会影响收件箱内容。
